# Repo público: o lockfile não pode carregar o registry interno de ninguém.
# Sem isto, `npm install` regrava as URLs do proxy do time em `resolved` — e elas vazam
# o hostname e quebram para quem está de fora.
registry=https://registry.npmjs.org/
